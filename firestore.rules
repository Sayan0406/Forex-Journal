rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Require the user to be logged in via Google Authentication to read or write data
      allow read, write: if request.auth != null;
    }
  }
}
